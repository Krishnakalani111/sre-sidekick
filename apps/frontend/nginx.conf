server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # SPA: unknown routes fall back to index.html (react-router client-side routing).
  location / {
    try_files $uri $uri/ /index.html;
  }
}
