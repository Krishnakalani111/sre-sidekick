# Hoist tooling (tsx, typescript, zod) so every workspace package resolves it at
# runtime without redeclaring. Keeps the just-in-time TS-source workspace deps simple.
shamefully-hoist=true
link-workspace-packages=true
